const pessoa = {
    nome: 'Eduardo' ,
    sobrenome: 'Santos'
};

// For clássico - Geralmente com iteráveis (array ou strings)
// For in - Retorna o indice ou chave (string, array, ou objetos)
// For of - Retorna o valore em si (iteráveis, arrays, ou strings)

for (let i = 0; i<nomes.lenght; i++)    {
    console.log(nomes);
}